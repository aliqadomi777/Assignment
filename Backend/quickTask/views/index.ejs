<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1><%- title %></h1>
    <p><%- seconds %></p>

    <% if (seconds % 2 === 0) { %>
    <p>No Items</p>
    <% } else { %>
    <ul>
      <% for (let i = 0; i < items.length; i++) { %>
      <li><%= items[i] %></li>
      <% } %>
    </ul>
    <% } %> <%- htmlContent %> <%- include("footer") %>
  </body>
</html>
